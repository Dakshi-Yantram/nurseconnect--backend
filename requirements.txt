fastapi==0.110.1
uvicorn==0.25.0
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4
email-validator==2.3.0
PyJWT==2.12.1
bcrypt==4.1.3
passlib==1.7.4
python-multipart==0.0.27
SQLAlchemy==2.0.49
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.18.4
redis==7.4.0
celery==5.6.3
# The [http2] extra pulls in `h2`. APNs only speaks HTTP/2, so plain httpx
# cannot talk to it — VoIP pushes fail at connect time without this.
httpx[http2]==0.28.1
pywebpush==2.0.3
# Android call ringing (data-only high-priority FCM messages).
firebase-admin==7.1.0
# PyJWT signs the APNs auth token with ES256, which needs `cryptography`
# present; PyJWT itself does not depend on it.
cryptography==46.0.3
websockets==16.0
starlette==0.37.2
python-jose==3.5.0
requests==2.33.1
typing_extensions==4.15.0
tzdata==2026.2
pymongo==4.5.0
motor==3.3.1
boto3==1.43.5
cloudinary==1.44.2
# E-prescription generation: PDF with the doctor's signature stamped on it,
# plus a QR code (linking to the public verify endpoint) embedded in the PDF.
reportlab==4.2.5
qrcode[pil]==8.0
Pillow==11.0.0
stripe==15.1.0
razorpay==2.0.1
pytest==9.0.3
pytest-asyncio==1.4.0
resend
gunicorn==23.0.0